# Changelog

## [1.1.0] - 2026-06-12

### ✨ 新功能

- **单张图片操作菜单**：点击图片弹出「预览 / 移动到」快捷菜单，无需进入多选模式
- **全屏预览移动**：全屏查看图片时可直接点击移动按钮移至自建文件夹
- **文件夹删除（含图片）**：文件夹列表页新增 🗑️ 按钮，点击后需二次确认，一键清理整个文件夹
- **拖放移动**：长按文件夹卡片拖动到「自建」Tab 可直接移入自建文件夹，拖动时有高亮提示和浮层预览
- **自动定时清理**：支持配置「每天 / 每周某天 / 每月某日」自动清理指定类型文件夹（相机、截图、视频、第三方图片等），凌晨 2:00 后台执行
- **多选一键移动**：多选模式下可选择已有文件夹移动，或一键移动到新建自建文件夹

### 🎨 UI 优化

- 文件夹封面图加载改用 Landscapist-Coil，支持 shimmer 骨架屏 + crossfade 淡入动画
- 图片选中动画升级为弹簧物理效果（Spring DampingRatio）
- 加载中状态改用 shimmer 骨架屏取代菊花转圈
- FAB 切换 Tab 时加入 scaleIn/scaleOut 动画
- 文件夹卡片圆角、封面比例、名称行优化
- 全屏查看器支持双指缩放、双击放大

### 🛠️ 稳定性 & 兼容性

- 修复 `queryFromUri()` 在部分设备上因列不存在导致 crash 的问题（改用 `getColumnIndex` + 校验）
- 修复 `deleteMedia()` 在 Android 11+（API 30+）上静默失败的问题（现在正确处理 SecurityException）
- 修复 `SimpleDateFormat` 在协程并发场景下非线程安全的问题（改用 ThreadLocal）
- 修复 `error` 消息为 null 时 UI 展示崩溃的问题（改用 `e.message ?: e.toString()`）
- 修复复制图片时 `extractMediaId` 返回 -1L 写入数据库的问题（加入 `id > 0` 校验）
- AutoCleanWorker 加入完整的错误处理和日志，不再静默忽略删除失败
- `HomeScreen.filteredFolders()` 改用 `derivedStateOf` 缓存，减少不必要的重计算
- 数据库版本升级至 v2（新增 `clean_rules` 表，带 Migration）

---

## [1.0.0] - 2026-06-10

### 首次发布

- 📷 **智能自动分类**：相机照片、截图、相机视频按日期自动分组
- 📱 **三方 App 图片聚合**：微信、微博等 App 图片按应用名聚合为独立文件夹
- 📁 **自建文件夹**：自由创建、重命名、删除文件夹，支持移入/复制图片
- ✅ **批量操作**：长按进入多选模式，支持移动、复制、删除
- 📡 **局域网传图**：开启 HTTP 服务，扫码或输入地址在电脑浏览器直接下载图片
- 🔒 **纯本地运行**：所有数据留在设备或局域网内，不上传任何内容
